// const { GPT4All } = require("gpt4all");

//  const modelName = "qwen2-1.5b-instruct.gguf"; // <-- match your actual filename
// let ai = null;

// async function initAI() {
  // if (!ai) {
   // ai = new GPT4All(modelName);
    //  await ai.init();
   // await ai.open();
   //  console.log("✅ AI Model Loaded:", modelName);
//  }
// }

// async function chatWithAI(message) {
   // await initAI();

  // const response = await ai.chatCompletion([
    // {
     //  role: "system",
      // content:
        // "You are a Smart Library AI Assistant. Recommend books based on user interests, age, and reading history. Respond in a friendly helpful tone."
    // },
   //  { role: "user", content: message }
//   ]);

  // return response.choices[0].message.content;
// }

 // module.exports = { initAI, chatWithAI };
